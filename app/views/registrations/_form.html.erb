<%= simple_form_for [@show, @registration], html: {class: 'form-horizontal'} do |f| %>
	<%= f.input :name %>
	<%= f.input :email %>
	<%= f.button :submit, 'Anmelden', class: 'btn-primary' %>
	<%= link_to 'Zurück', :back, class: 'btn' %>
<% end %>