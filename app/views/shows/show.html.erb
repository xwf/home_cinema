<div id="suggestions_header" class="page-header">
	<h1>Filmvorschläge für <%=l! @show.date.to_date %></h1>
</div>
<%= render @show.movie_suggestions, edit: false %>
<div id="actions">
	<%= link_to 'Jetzt anmelden!', new_show_registration_path(@show), class: 'btn btn-primary' %>
	<%= link_to 'Zurück zur Übersicht', shows_path, class: 'btn' %>
</div>