<div class="show-container">
	<h3><%=l! show.date, format: :long %></h3>
	<p><%= show.text %></p>
	<h4>Filmvorschläge</h4>
	<div class="suggestions clearfix">
		<%= render partial: 'movie_suggestions/preview', collection: show.movie_suggestions %>
	</div>
	<div class="actions">
		<%= render (@admin ? 'shows/admin_actions' : 'shows/user_actions'), show: show %>
	</div>
</div>